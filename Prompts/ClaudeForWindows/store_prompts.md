# Terminal

mcp install server.py

# Store prompts

/list_stores

layout recommendations for this store

Take these results and create rows in my Notion ‘Layout Actions’ database using Notion tools

Summarize the new layout actions into a ‘Store Playbook - Upcoming Weekend’ page with sections: Overview, Key Moves, Expected Impact.

Query my Notion Layout Actions database for rows updated in the last 3 days. For each store found, call the weather-smart-merchandiser server to get suggested layout actions. Then add any new actions to the Layout Actions database in Notion (with correct Category relations (match by exact Category name in DIY Categories & Layout)), skipping duplicates based on Action title + Start Date. 


# Dev prompts

What Notion tools do you have available? Specifically, can you query a database and delete pages?